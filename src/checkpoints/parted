for symlink in $(find /sys/block/ -maxdepth 1 -type l); do
  dev="/dev/$(cut -d/ -f4 <<< $symlink)"
  _output="$(parted -s $dev unit s print 2> /dev/null)"
  [ $? -eq 0 ] && echo -e "${_output}\n" | tr -d '\033'
done
